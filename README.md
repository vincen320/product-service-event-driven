# product-service-mongodb-event-driven
This service act as producer to send data to queue then will be consume by email-service. Using NSQ as message broker.

# Architecture
Controller -> Service -> Repository
